CREATE TABLE snippets (
id INTEGER NOT NULL PRIMARY KEY,
title text NOT NULL,
content TEXT NOT NULL,
created DATETIME NOT NULL,
expires DATETIME NOT NULL
);

 insert into snippets (title, content, created, expires) values("snippet2", "Functions accept modifiers to adjust dates/times:", datetime('now'), datetime('now', '15 days'));

 INSERT INTO snippets (title, content, created, expires)
VALUES('puki forever', 'to specify values for all columns in the value list. The number of values in the value list must be the same as the number of columns in the column list.', UTC_TIMESTAMP(), DATE_ADD(UTC_TIMESTAMP(), INTERVAL 7 DAY));

 INSERT INTO snippets (title, content, created, expires)
VALUES('puki forever', 'to specify values for all columns in the value list be the same as the number of columns in the column list.', datetime('now', 'localtime'), datetime('now', '+7 DAY', 'localtime'));

CREATE TABLE users (
id INTEGER NOT NULL PRIMARY KEY autoincrement,
name VARCHAR(255) NOT NULL,
email VARCHAR(255) NOT NULL,
hashed_password CHAR(60) NOT NULL,
created DATETIME NOT NULL,
unique(email)
);